#!/usr/bin/env bash
# direnv configuration for happy-farm

# Expose the uv-managed project environment without creating a second venv.
export VIRTUAL_ENV="$PWD/.venv"
PATH_add "$VIRTUAL_ENV/bin"

# Add scripts to PATH for convenience.
PATH_add scripts
