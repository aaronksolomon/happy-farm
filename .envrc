#!/usr/bin/env bash
# direnv configuration for happy-farm

# Activate uv virtual environment (uv creates .venv automatically)
source_env .venv/bin/activate

# Add scripts to PATH for convenience
PATH_add scripts
